# abspath

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run main.ts
```

Install executable:

copies executable to ~/.local/bin

```bash
bun run build && bun run install
```
