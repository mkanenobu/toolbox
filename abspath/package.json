{
  "name": "abspath",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/main.ts --compile --outfile abspath",
    "install": "mv abspath ~/.local/bin/abspath",
    "start": "bun run ./src/main.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}